{
  "name": "dara-ai-stylist-backend",
  "version": "1.0.0",
  "description": "AI color-analysis backend using Gemini Vision, with cost-controlling caching, image validation, and a real (non-hardcoded) deterministic fallback.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^7.1.0",
    "sharp": "^0.34.5"
  },
  "engines": {
    "node": ">=18"
  }
}
