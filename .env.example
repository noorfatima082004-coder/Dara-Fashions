# Local dev only, pointing at the standalone server/ process.
# Leave this UNSET on Vercel — the API deploys as a serverless function on
# the same domain, so a relative /api/... path is used automatically.
VITE_API_BASE_URL=http://localhost:3000
